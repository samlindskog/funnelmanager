apiVersion: v1
kind: Service
metadata:
  name: leads
  labels:
    app.kubernetes.io/name: leads
    app.kubernetes.io/part-of: funnelmanager
spec:
  selector:
    app.kubernetes.io/name: leads
  ports:
    - name: http
      port: 8001
      targetPort: 8001
