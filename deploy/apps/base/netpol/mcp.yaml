# mcp is internal-only: never routed from the gateway and, for now, with no
# ingress allowlist at all (default-deny keeps it dark; only the monitoring
# scrape policy reaches it). When in-cluster agent clients arrive, grant them
# ingress explicitly by label, e.g.:
#
#   ingress:
#     - from:
#         - podSelector:
#             matchLabels:
#               app.kubernetes.io/name: agent-runner
#       ports:
#         - protocol: TCP
#           port: 8003
#
# Egress: leads, search, jobs (the backends its tools front) and Keycloak. DNS via allow-dns.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp
  labels:
    app.kubernetes.io/name: mcp
    app.kubernetes.io/part-of: funnelmanager
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mcp
  policyTypes:
    - Ingress
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: leads
      ports:
        - protocol: TCP
          port: 8001
    # search + jobs: MCP tools call their /api/{svc}/mcp/v1/* endpoints
    # (OPA still gates on the mcp->{svc} exchange edge + workload identity).
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: search
      ports:
        - protocol: TCP
          port: 8000
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: jobs
      ports:
        - protocol: TCP
          port: 8005
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: identity
          podSelector:
            matchLabels:
              app.kubernetes.io/name: keycloak
      ports:
        - protocol: TCP
          port: 8080
