# search: reached from the ingress gateway, jobs (job stream/control), and mcp
# (search tools); talks to leads, its own Postgres (app-db), and Keycloak
# (token exchange). DNS via allow-dns.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: search
  labels:
    app.kubernetes.io/name: search
    app.kubernetes.io/part-of: funnelmanager
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: search
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # knowledge's sync connectors (GET-only, service identity holding
    # knowledge-internal; canary label shares the read path). NetworkPolicy is
    # enforced on BOTH ends — without this ingress leg the knowledge sidecar's
    # dial is refused and surfaces as HTTP 503 (ops-runbook §2).
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: knowledge
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: knowledge-canary
      ports:
        - protocol: TCP
          port: 8000
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-ingress
      ports:
        - protocol: TCP
          port: 8000
    # jobs subscribes to search's /internal/jobs/v1 stream and proxies its
    # control API (search is the v1 job producer). OPA still gates the
    # request on the jobs-internal role + the jobs->search exchange edge.
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: jobs
      ports:
        - protocol: TCP
          port: 8000
    # mcp tools call search's /api/search/mcp/v1/* (OPA gates on the mcp->search
    # exchange + the acting principal's search-access grant).
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: mcp
      ports:
        - protocol: TCP
          port: 8000
  egress:
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: leads
      ports:
        - protocol: TCP
          port: 8001
    # mail: search reads the contacted set (GET /api/mail/contacts/contacted)
    # for the exclude_contacted filter (search->mail exchange edge).
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: mail
      ports:
        - protocol: TCP
          port: 8004
    # CNPG labels instance pods with cnpg.io/cluster, not app.kubernetes.io/name.
    - to:
        - podSelector:
            matchLabels:
              cnpg.io/cluster: app-db
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: identity
          podSelector:
            matchLabels:
              app.kubernetes.io/name: keycloak
      ports:
        - protocol: TCP
          port: 8080
